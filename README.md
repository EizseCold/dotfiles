# dotfiles
My personal shell configuration for Linux environments.

## Usage

git clone https://github.com/EizseCold/dotfiles.git
bash dotfiles/install.sh
source ~/.bashrc

## What's included

- bashrc — shell aliases for navigation, git, and safety nets
- install.sh — symlinks config files into place
